# Bookshelf App (読書管理アプリケーション)

React と TypeScript を用いて構築した、個人向けの読書管理アプリケーションです。  
開発者が自ら設計と最終決定を行い、複数の AI ツール（Gemini / GitHub Copilot Chat）を役割に応じて活用しながら、設計思想やプロンプト履歴を記録する開発プロセスを採用しています。

---

## 🛠 技術スタック (Tech Stack)

* **フロントエンド**: React 18 / TypeScript
* **ビルドツール**: Vite
* **ランタイム環境**: Node.js v24.21.0
* **開発支援・設計パートナー (AI)**:
  * **Gemini**: 概念設計の壁打ち、課題整理、ドキュメント構成提案、コードレビュー・動作確認アドバイス
  * **GitHub Copilot Chat**: 設計仕様に基づくTypeScript/Reactコードの生成・実装指示の実行

---

## 🤖 AIの活用と役割分担

本プロジェクトでは、AIに全面的に依存するのではなく、以下のように明確な役割分担を行って開発を進めています。

```text
[ 開発者 (人間) ]
  ├── 設計のアイデア出し・最終決定
  └── AIが生成したコードの最終レビュー・動作確認
       │
       ├─── [ Gemini ]
       │      ・要件定義や型設計、State設計の検討パートナー
       │      ・開発ドキュメント（README/設計書/プロンプト集）の構成案作成
       │      ・生成コードの客観的なレビューとアドバイス
       │
       └─── [ GitHub Copilot Chat ]
              ・作成されたプロンプトに基づく具体的なソースコード生成
```
---

## 📐 設計思想とアーキテクチャ

保守性・再利用性・アクセシビリティを考慮した設計を行っています。

### 1. 状態（State）と派生データの一元管理
* データと主要ロジックは Custom Hook (`src/hooks/useBooks.ts`) に集約。
* 表示用の `filteredBooks` は独立した State にせず、`books`・`searchQuery`・`filterStatus` から**レンダリング時に動的計算（派生）**することで、State 同期の不整合（バグ）を排除。

### 2. コンポーネント設計（単一責任の原則）
* **`BookCard`**: 1冊の読書カードの可視化とアクション（削除・ステータス変更）。セマンティックなマークアップ (`<article>`, `<dl>`) とアクセシビリティ（`aria-label` 等）に配慮。
* **`BookFilter`**: キーワード検索とステータス絞り込みのコントロール。
* **`App`**: 全体のレイアウトと Hook からのデータ流し込みのみを担うトップレベルコンポーネント。

---

## 📁 ディレクトリ構造 & 開発ドキュメント

開発プロセスや AI への指示プロンプト履歴はすべてドキュメントとして記録・管理しています。

```text
bookshelf-app/
├── docs/
│   ├── design/                 # 設計仕様書・アーキテクチャ定義
│   └── prompts/                # AIへの指示プロンプト履歴
├── src/
│   ├── components/             # UIコンポーネント (BookCard, BookFilter)
│   ├── hooks/                  # Custom Hook (useBooks)
│   ├── mocks/                  # モックデータ (books)
│   ├── types/                  # 型定義 (book.ts)
│   ├── App.tsx
│   └── main.tsx
└── README.md
