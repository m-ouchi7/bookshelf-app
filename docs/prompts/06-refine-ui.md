# Prompt 06: UIスタイリングと仕様変更（カード幅・追加UI・削除制限・タブ式ステータス）の実装指示

## 実行日
2026-09-20

## 指示文（GitHub Copilot Chatへ投入）

このプロジェクトでは、設計と最終決定は私が行います。
あなたは「実装者」として以下の設計に従ってコードを作成してください。

【制約】
- 設計（型定義や関数の役割）を勝手に変更しないこと
- 新しい外部ライブラリを追加しないこと
- 実装前に不明点や懸念があれば質問すること

【指示内容】
1. カードの横幅変更:
   - `BookCard`（一覧のカード）の最大横幅を `308px` に設定してください。

2. 本の追加UIの刷新:
   - 従来のトグル式による追加フォームを廃止し、「本を追加」ボタンを押すと新規追加用の画面へ移行するようにしてください。
   - 本を追加する画面のUIは既存の編集画面と同様のコンポーネントを利用してください。

3. 削除機能の移動と確認ダイアログ:
   - 一覧の `BookCard` から削除ボタンを削除してください。
   - 削除ボタンは詳細画面でのみ表示するようにしてください。
   - 削除ボタンを押した際、ブラウザ標準の確認ダイアログ（例: `window.confirm("本当に削除しますか？")`）を表示し、OKの場合のみ削除を実行してTOPへリダイレクトしてください。

4. ステータス変更の仕様変更（タブ切り替え）:
   - ステータス（例: 未読 / 読中 / 読了）の変更は、一覧やアコーディオンではなく、編集モード時に**タブ切り替え形式**で行えるように改修してください。

5. 日付と時刻の表示:
   - 登録日時と更新日時を年月日時分の形式にしてください。
   - 読了日は年月日の表示にしてください。

6. フォント
   - メモの中の文字の大きさを他と揃えてください。
   - 文字の色を全体的に濃くしてください。
